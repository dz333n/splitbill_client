import 'package:flutter/material.dart';

class NoChosenProducts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Cannot find any products for this bill 🤔"),
    );
  }
}
