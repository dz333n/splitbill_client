import 'package:flutter/material.dart';

class DebtsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Debts")),
      body: Center(child: Text("Debts")),
    );
  }
}
